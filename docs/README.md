1. car를 만든다.
   1. 1111 전체에 대해 null값 들어왔을 때 validation 추가
2. 자동차에는 Name과 Index를 가지며 원시값을 포장한다.
   1. 클래스로 분리한 Name, Index의 validation을 진행한다.
      1. name: 이름은 5글자 이하만 가능하다.
      2. index: 전까지 String값으로 전달받은 다음에 숫자 형태가 맞는지 확인한다. -> 아니면 예외
3. cars를 만들어 자동차들을 가진다.
   1. 자동차들의 이름이 같은 경우 예외를 발생시킨다.
   2. 자동차 경주에 참여하는 자동차들의 대수를 제한한다 -> public constant 클래스로 관리하고, 0 인 경우 무제한이 된다 -> 상수값, 주석으로 표기
4. Movement를 사용하여 자동차가 움직일 횟수를 만든다.
   1. String으로 입력값 받으며 숫자가 아닌경우와 1보다 작은 경우 예외를 발생시킨다.
5. RacingGame 클래스를 생성하여 게임을 진행할 수 있는 클래스를 생성한다 -> cars와 movement 클래스를 가진다.
   1. tryNo을 반복하면서 자동차를 움직인다.
6. 자동차를 움직이도록 car.move() 메서드를 구현한다. -> 도메인에 해당 메시지를 전달한다.
   1. move() 메서드를 구현하며 랜덤 값에 따라 움직이도록 한다.
      1. 11111 car를 움직이며 Index 객체는 final 값으로 caching값을 전달한다.
   2. 랜덤값을 외부에서 전달 받을 수 있게 하여 테스트 가능한 코드로 만든다. -> 우선 오버로딩으로 만든다.
   3. 랜덤값이 0, 1 이 아닌 경우 예외를 발생시킨다.
7. randomNumber를 만드는 것을 전략패턴으로 만들고 테스트를 한다.
   1. random으로 움직임 값을 만드는 것과, 순차적으로 움직임을 만드는 구현체를 만들어 테스트한다.
   2. 1111 전략 패턴으로 만든 이후 functional interface로 변경한다.
8. ** 움직임 중간에 계속해서 결과를 출력해야 하므로 어떻게 가져갈지 구조 개선 생각
   1. 1111 continuable을 만들고, Movement또한 캐시 사용하고, -> 데이터가 없으면 Map에 넣고 캐시로 사용하고, 데이터가 있는 경우에는 바로 가져와서 사용하도록 
   2. 1111 마찬가지로 tryNo도 캐시 적용
   3. tryNo를 외부에서 for문을 돌려야 하는데,, 이렇게 하는 경우 tryNo 클래스 자체는 데이터의 validation만 하는 역할을 가진다.
   4. for문으로 돌리는 것이 아니라, tryNo를 하나씩 차감해가면서 -> 0이면 게임이 끝나도록 -> 객체에서 상태를 가지고 있어야한다.
   5. final 값으로 값을 가지는 형태가 아니라, int형이기 때문에 하나씩 tryNo를 줄여가며 출력
   6. Controller / service는 racingGame객체에 move() 메서드만 호출하고, 끝난경우 true false 리턴
   7. 메서드를 하나 만들어 tryNo가 0인 경우 게임이 끝나도록 (true/false) 출력 메서드 생성
9. 게임이 끝난 경우 Cars객체를 Winner 객체에 (service에서) 전달한다.
   1. 전달 받은 car 객체를 가지고 반복하면서 우승자를 출력한다. -> 동시 우승한 사람이 있을 수 있으므로 list 형태로 출력한다.
   2. 우승자 생성하는 로직을 가능하다면 인터페이스로 만들고, di를 통해 구현체가 우승자를 출력할 수 있도록 만든다. 
   3. 1111 car를 기준으로 사용하는데 winner를 사용할 수 있는지 확인한다.
10. 예외가 발생한 경우 ERROR 문구를 출력하고, 다시 입력받는다.
    1. 래퍼클래스로 try-with-resource 구문을 사용한다.
11. 도메인에 메시를 보낼 Controller, service를 생성한다.
12. 화면을 출력할 클래스를 구현한다.
13. 
14. final로 선언하면서 객체 이동시에는 새로 객체를 생성해서 전달, 캐싱 적용
15. 인터페이스화
16. 
17. 자동차가 아닌 것도 레이싱 게임을 할 수 있으므로 T로 받을까?
18. 중간에 게임에 탈락
19. 
20. 인스턴스 이름을 기재하지 않도록 수정